<li id="<%= dom_id(prospect) %>">
  <%= link_to prospect, user_path(prospect)  %> (<%= link_to_remote "add", 
        { :url => register_user_movie_night_path(prospect, movie_night), :method => :post } %>)
</li>